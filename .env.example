POSTGRES_DB=jobmatcher
POSTGRES_USER=jobmatcher
POSTGRES_PASSWORD=jobmatcher
DATABASE_URL=postgresql+psycopg2://jobmatcher:jobmatcher@postgres:5432/jobmatcher
AIRFLOW_DATABASE_URL=postgresql+psycopg2://jobmatcher:jobmatcher@postgres:5432/jobmatcher
AIRFLOW_USERNAME=admin
AIRFLOW_PASSWORD=admin
AIRFLOW_SECRET_KEY=change-me
STARTUP_DAG_MAX_ATTEMPTS=30
STARTUP_DAG_RETRY_DELAY=5
STARTUP_DAG_IDS=linkedin_jobs_ingestion_startup,etat_geneve_jobs_ingestion_startup
ETAT_GENEVE_RSS_URL=https://www.ge.ch/rss/offres-emploi-etat-geneve?departement=0&domaine_activite=0&classe_fonction_min=0&type_contrat=0&taux_activite_max=0
# Optionally set a shared logical startup identifier when several startup-trigger
# processes may coordinate against the same Airflow metadata database.
# AIRFLOW_STARTUP_ID=20260711T084500Z-123
# Rebuild the image after changing this value so the selected model is preloaded in cache.
EMBEDDING_MODEL_NAME=sentence-transformers/paraphrase-multilingual-MiniLM-L12-v2
